---
phase: TRAFFICPRO-02-e2e-unit-coverage
plan: 02
type: execute
wave: 1
depends_on: [02-01]
files_modified: [src/store/useStore.test.js, server/tests/api.test.js]
autonomous: true
requirements: [TEST-04]
must_haves:
  truths:
    - "Vitest runs and asserts store changes and API endpoints successfully"
  artifacts:
    - path: "src/store/useStore.test.js"
      provides: "Unit tests verifying Zustand store states and action triggers"
    - path: "server/tests/api.test.js"
      provides: "Integration tests validating Express API controllers"
---

<objective>
Implement frontend unit tests validating Zustand global state transitions and backend integration tests validating controller endpoints.
</objective>

<tasks>
<task type="auto">
  <name>Create Zustand store unit tests</name>
  <files>src/store/useStore.test.js</files>
  <action>Create frontend unit tests asserting products list loading, settings updates, and campaign state toggles inside Zustand store.</action>
  <verify>test -f src/store/useStore.test.js</verify>
  <acceptance_criteria>
    - useStore.test.js covers main state modification actions
  </acceptance_criteria>
  <done>Zustand store unit tests implemented</done>
</task>

<task type="auto">
  <name>Create backend API integration tests</name>
  <files>server/tests/api.test.js</files>
  <action>Create backend Express unit/integration tests confirming correct 200/401 payloads on auth endpoints with a mock database context.</action>
  <verify>test -f server/tests/api.test.js</verify>
  <acceptance_criteria>
    - api.test.js asserts controller response codes correctly
  </acceptance_criteria>
  <done>Backend integration tests implemented</done>
</task>
</tasks>

<verification>
- npx vitest run
</verification>

<success_criteria>
- Vitest executes all unit tests successfully passing the assertions
</success_criteria>
