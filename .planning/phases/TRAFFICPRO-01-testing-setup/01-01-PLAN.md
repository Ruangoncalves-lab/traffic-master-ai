---
phase: TRAFFICPRO-01-testing-setup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [package.json, vite.config.js, src/test/setup.js]
autonomous: true
requirements: [TEST-01]
must_haves:
  truths:
    - "Vitest runs and executes unit tests successfully"
  artifacts:
    - path: "vite.config.js"
      provides: "Vitest configuration block using JSDOM"
    - path: "src/test/setup.js"
      provides: "Testing library setup and DOM matching expectations setup"
  key_links: []
---

<objective>
Configure Vitest test runner on the frontend to enable unit and component testing.

Purpose: Set up the core testing infrastructure for verifying Zustand stores and React hooks.
Output: Configured vite.config.js, setup file, and npm test scripts.
</objective>

<execution_context>
@~/.gemini/antigravity/get-shit-done/workflows/execute-plan.md
@~/.gemini/antigravity/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/TRAFFICPRO-01-testing-setup/TRAFFICPRO-01-RESEARCH.md
@vite.config.js
@package.json
</context>

<tasks>
<task type="auto">
  <name>Install Vitest and testing packages</name>
  <files>package.json</files>
  <read_first>package.json, .planning/phases/TRAFFICPRO-01-testing-setup/TRAFFICPRO-01-RESEARCH.md</read_first>
  <action>Install Vitest, jsdom, @testing-library/react, @testing-library/jest-dom, and cross-env with --legacy-peer-deps to prevent React 19 package version conflicts. Add "test": "vitest run" to package.json scripts block.</action>
  <verify>npm run test -- --version</verify>
  <acceptance_criteria>
    - package.json contains "vitest" in devDependencies
    - package.json contains "test": "vitest run" in scripts
  </acceptance_criteria>
  <done>Testing dependencies installed and scripts mapped</done>
</task>

<task type="auto">
  <name>Update Vite config with Vitest block</name>
  <files>vite.config.js</files>
  <read_first>vite.config.js</read_first>
  <action>Add /// &lt;reference types="vitest" /&gt; import at the top of vite.config.js. Update the exported config object to include a "test" configuration block specifying globals: true, environment: "jsdom", and setupFiles: "./src/test/setup.js".</action>
  <verify>grep -q "environment: 'jsdom'" vite.config.js</verify>
  <acceptance_criteria>
    - vite.config.js contains environment configuration for jsdom
    - vite.config.js refers setupFiles to "./src/test/setup.js"
  </acceptance_criteria>
  <done>Vite configuration upgraded to support Vitest</done>
</task>

<task type="auto">
  <name>Create Vitest setup file</name>
  <files>src/test/setup.js</files>
  <read_first>.planning/phases/TRAFFICPRO-01-testing-setup/TRAFFICPRO-01-RESEARCH.md</read_first>
  <action>Create a test setup script at src/test/setup.js importing @testing-library/react cleanup and @testing-library/jest-dom/matchers. Extend Vitest's expect with the testing library DOM matchers, and run afterEach cleanup.</action>
  <verify>test -f src/test/setup.js</verify>
  <acceptance_criteria>
    - src/test/setup.js extends expect with jest-dom matchers
    - src/test/setup.js executes cleanup in afterEach hook
  </acceptance_criteria>
  <done>Setup file initialized</done>
</task>
</tasks>

<verification>
- [ ] npm run test runs without errors
</verification>

<success_criteria>
- All Vitest config files created
- Script execution passes
</success_criteria>

<output>
After completion, create `.planning/phases/TRAFFICPRO-01-testing-setup/01-01-SUMMARY.md`
</output>
