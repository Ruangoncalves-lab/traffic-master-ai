---
phase: TRAFFICPRO-03-database-robustness
plan: 02
type: execute
wave: 1
depends_on: [03-01]
files_modified: [supabase/migrations/20251203_create_core_tables.sql, supabase/migrations/20251204_create_meta_tables.sql, supabase/migrations/20251205_meta_integration_v2.sql]
autonomous: true
requirements: [DB-02]
must_haves:
  truths:
    - "SQL migrations use CREATE TABLE IF NOT EXISTS and ALTER TABLE ADD COLUMN IF NOT EXISTS without dropping production tables"
  artifacts:
    - path: "supabase/migrations/20251203_create_core_tables.sql"
      provides: "Core SaaS database tables migration"
---

<objective>
Audit and enforce the non-destructive SQL migration policy for Supabase database.
</objective>

<tasks>
<task type="auto">
  <name>Apply policy to migrations</name>
  <files>supabase/migrations/*.sql</files>
  <action>Add structured non-destructive comment headers to the SQL migrations inside supabase/migrations/.</action>
  <verify>grep -q "POLICY" supabase/migrations/20251204_create_meta_tables.sql</verify>
  <acceptance_criteria>
    - Migration files contain non-destructive directive check comments
  </acceptance_criteria>
  <done>Non-destructive policies documented and applied</done>
</task>
</tasks>

<verification>
- Check migrations manually for any destructive query commands
</verification>

<success_criteria>
- Migrations executed successfully on Supabase without dropping tables
</success_criteria>
